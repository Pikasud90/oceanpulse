# OceanPulse core dependencies.
# Deliberately small: no database server, no geospatial C extensions, no
# Docker. Everything here installs from wheels on Windows, macOS and Linux.

dash>=2.17,<4
plotly>=5.22
httpx>=0.27
pydantic>=2.7
pandas>=2.2
numpy>=1.26
pyarrow>=16.0
